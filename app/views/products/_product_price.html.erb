<div class="flex flex-col">
  <div class="flex gap-2 items-center">
    <p class="text-sm text-gray-500">
      Цена
    </p>

    <%= link_to edit_price_location_product_path(location, product),
      data: { turbo_frame: :product_price, turbo_stream: true },
      title: "Редактировать цену" do
    %>
      <%= inline_svg_tag "pencil-square.svg", class: "text-gray-500" %>
    <% end %>
  </div>

  <p><%= product.price %></p>
</div>
